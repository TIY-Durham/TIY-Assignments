(function (window) { //this function takes window object which represents a window containing the DOM document. 
		     //I believe the function(window) is saying that in this function the code will be part of the DOM document and be displayed in the browser as part of the page.  	
	'use strict';

	// Your starting point. Enjoy the ride!

})(window);
